import fs from "node:fs";
import path from "node:path";

const port=Number(process.env.PCS_CDP_PORT||9234),url=process.env.PCS_TEST_URL||"http://127.0.0.1:8765/projects/Planetary-common-state/PCS_OBSERVATORY/?v=phase-4b",outputDir=process.env.PCS_PHASE4B_OUTPUT||path.join(process.cwd(),"test-results","deep-space-phase-4b-local");fs.mkdirSync(outputDir,{recursive:true});
const target=await fetch(`http://127.0.0.1:${port}/json/new?${encodeURIComponent(url)}`,{method:"PUT"}).then(r=>r.json()),socket=new WebSocket(target.webSocketDebuggerUrl),pending=new Map(),consoleErrors=[],networkFailures=[];let sequence=0;
await new Promise((resolve,reject)=>{socket.addEventListener("open",resolve,{once:true});socket.addEventListener("error",reject,{once:true});});
socket.addEventListener("message",event=>{const message=JSON.parse(event.data);if(message.id&&pending.has(message.id)){const item=pending.get(message.id);pending.delete(message.id);return message.error?item.reject(new Error(message.error.message)):item.resolve(message.result);}if(message.method==="Runtime.exceptionThrown")consoleErrors.push(message.params.exceptionDetails.exception?.description||message.params.exceptionDetails.text);if(message.method==="Network.loadingFailed"&&!message.params.canceled)networkFailures.push({requestId:message.params.requestId,error:message.params.errorText});});
const send=(method,params={})=>new Promise((resolve,reject)=>{const id=++sequence;pending.set(id,{resolve,reject});socket.send(JSON.stringify({id,method,params}));});
const evaluate=async expression=>{const result=await send("Runtime.evaluate",{expression,awaitPromise:true,returnByValue:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.exception?.description||result.exceptionDetails.text);return result.result.value;};
const wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));const waitFor=async(expression,timeout=60000)=>{const started=Date.now();while(Date.now()-started<timeout){if(await evaluate(`Boolean(${expression})`))return;await wait(150);}throw new Error(`Timeout: ${expression}`);};const assert=(value,message)=>{if(!value)throw new Error(message);};
async function screenshot(name){const result=await send("Page.captureScreenshot",{format:"png",captureBeyondViewport:false});const file=path.join(outputDir,name);fs.writeFileSync(file,Buffer.from(result.data,"base64"));return file;}
const visiblePhase4=()=>evaluate(`(()=>{const canvas=cesiumViewer.scene.canvas,items=[];for(const collection of cesiumViewer.scene.primitives._primitives||[]){if(collection.show===false)continue;for(let i=0;i<(collection.length||0);i++){const point=collection.get?.(i),record=point?.id?.phase4Object;if(!record||point.show===false||!Number.isFinite(point.pixelSize))continue;const projected=Cesium.SceneTransforms.worldToWindowCoordinates(cesiumViewer.scene,point.position);items.push({id:record.id,name:record.canonicalName,type:record.objectType,x:projected?.x??null,y:projected?.y??null,onScreen:Boolean(projected&&projected.x>=0&&projected.x<=canvas.clientWidth&&projected.y>=0&&projected.y<=canvas.clientHeight),pixelSize:point.pixelSize});}}return {items,onScreen:items.filter(item=>item.onScreen&&item.pixelSize>0).length};})()`);
await Promise.all([send("Runtime.enable"),send("Network.enable"),send("Page.enable"),send("Performance.enable")]);await send("Emulation.setDeviceMetricsOverride",{width:1440,height:1000,deviceScaleFactor:1,mobile:false});await waitFor("window.PCSDeepSpaceManager&&document.querySelector('#intro-enter')");await evaluate("document.querySelector('#intro-enter')?.click()");await waitFor("!document.body.classList.contains('intro-active')");await waitFor("document.querySelector('.cesium-viewer')&&typeof cesiumViewer!=='undefined'&&cesiumViewer&&!cesiumViewer.isDestroyed()");if(!await evaluate("PCSDeepSpaceManager.debug().initialized"))await evaluate("PCSDeepSpaceManager.initialize({viewer:cesiumViewer,host:document.querySelector('#cesium-globe')})");await evaluate("PCSI18n.setLanguage('zh-TW');PCSDeepSpaceManager.open()");await waitFor("PCSDeepSpaceManager.isOpen()");
const initial=await evaluate("PCSDeepSpaceManager.debug()"),primitiveInitial=await evaluate("cesiumViewer.scene.primitives.length"),dataSourcesInitial=await evaluate("cesiumViewer.dataSources.length");assert(initial.viewerCount===1,"Viewer must remain one");
assert(await evaluate("PCSDeepSpaceManager.enterGalaxyGroups()"),"enter Phase 4A transition");await wait(900);await screenshot("01-nearby-groups-to-virgo-transition.png");
assert(await evaluate("PCSDeepSpaceManager.enterVirgo()"),"enter Virgo");await waitFor("PCSDeepSpaceManager.debug().virgo?.catalogPoints===1589");await wait(1200);const overviewDebug=await evaluate("PCSDeepSpaceManager.debug()"),overview=await visiblePhase4();assert(overviewDebug.scaleContext==="virgo"&&overviewDebug.virgo.catalogPoints===1589,"EVCC catalog primitives exist");assert(overview.onScreen>=1,"Virgo overview has a visible representation");await screenshot("02-virgo-overview.png");
const named={};for(const name of ["M87","M49","M86","M84","M60"]){const result=await evaluate(`PCSDeepSpaceManager.searchPhase4(${JSON.stringify(name)})`);assert(result?.objectType==="Galaxy",`${name} search`);named[name]={id:result.id,raDeg:result.raDeg,decDeg:result.decDeg,membershipStatus:result.membershipStatus};}
const m87=await evaluate("PCSDeepSpaceManager.searchPhase4('M87')");await wait(1000);const selected=await visiblePhase4(),selectedM87=selected.items.find(item=>item.id===m87.id);assert(selectedM87?.onScreen&&selectedM87.pixelSize>=13,"selected M87 remains visibly rendered across LOD");const card=await evaluate("({id:document.querySelector('[data-object-card]').dataset.objectId,text:document.querySelector('[data-ds-info]').textContent,title:document.querySelector('[data-object-card-title]').textContent})");assert(card.id===m87.id&&/Individual distance unavailable/.test(card.text)&&/Representative Visualization/.test(card.text)&&/EVCC member/.test(card.text),"M87 card distinguishes observation, membership, and representative shell");await screenshot("03-m87-focus-object-card.png");
const possible=await evaluate("PCSDeepSpaceManager.searchPhase4('EVCC 1')");assert(possible&&["Member","Possible member"].includes(possible.membershipStatus),"EVCC identity search resolves membership");
const cluster=await evaluate("PCSDeepSpaceManager.searchPhase4('Virgo Cluster')");assert(cluster?.objectType==="Galaxy Cluster"&&cluster.memberCount===1028&&cluster.possibleMemberCount===561,"cluster summary search");await wait(800);await screenshot("04-virgo-cluster-card.png");
const listenerCounts=()=>evaluate("({changed:cesiumViewer.camera.changed.numberOfListeners,moveStart:cesiumViewer.camera.moveStart.numberOfListeners,moveEnd:cesiumViewer.camera.moveEnd.numberOfListeners,postRender:cesiumViewer.scene.postRender.numberOfListeners,preRender:cesiumViewer.scene.preRender.numberOfListeners})");const cycles=[];for(let i=0;i<10;i++){assert(await evaluate("PCSDeepSpaceManager.enterGalaxyGroups()"),`cycle ${i} groups`);assert(await evaluate("PCSDeepSpaceManager.enterVirgo()"),`cycle ${i} virgo`);cycles.push({primitive:await evaluate("cesiumViewer.scene.primitives.length"),dataSources:await evaluate("cesiumViewer.dataSources.length"),listeners:await listenerCounts(),debug:await evaluate("PCSDeepSpaceManager.debug()")});}assert(cycles.every(item=>item.primitive===cycles[0].primitive&&item.dataSources===cycles[0].dataSources&&JSON.stringify(item.listeners)===JSON.stringify(cycles[0].listeners)&&item.debug.virgo.catalogPoints===1589),"10 cycles have no collection, data-source, or listener growth");
const expected={"zh-TW":["室女座星系團","搜尋","星系群、室女座星系團或星系"],en:["Virgo Cluster","Search","Galaxy group, Virgo cluster, or galaxy"],ja:["おとめ座銀河団","検索","銀河群・おとめ座銀河団・銀河"],ko:["처녀자리 은하단","검색","은하군, 처녀자리 은하단 또는 은하"]},languages={};for(const language of Object.keys(expected)){await evaluate(`PCSI18n.setLanguage(${JSON.stringify(language)})`);await wait(100);languages[language]=await evaluate("[document.querySelector('[data-ds-phase4-heading]').textContent,document.querySelector('[data-p4=search]').textContent,document.querySelector('[data-ds-phase4-search-input]').getAttribute('aria-label'),PCSDeepSpaceManager.debug().viewerCount]");assert(JSON.stringify(languages[language].slice(0,3))===JSON.stringify(expected[language]),`${language} exact Phase 4B UI`);assert(languages[language][3]===1,`${language} does not recreate Viewer`);}
await evaluate("PCSI18n.setLanguage('zh-TW')");await send("Emulation.setDeviceMetricsOverride",{width:390,height:844,deviceScaleFactor:2,mobile:true});await wait(350);const mobile=await evaluate("({overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,viewer:document.querySelectorAll('.cesium-viewer').length,cesiumCanvas:document.querySelectorAll('.cesium-widget canvas').length})");assert(!mobile.overflow&&mobile.viewer===1&&mobile.cesiumCanvas===1,"mobile single Viewer and no overflow");await screenshot("05-mobile-virgo.png");
await evaluate("PCSDeepSpaceManager.returnSolar()");const final=await evaluate("PCSDeepSpaceManager.debug()"),primitiveFinal=await evaluate("cesiumViewer.scene.primitives.length"),dataSourcesFinal=await evaluate("cesiumViewer.dataSources.length");assert(final.scaleContext==="solar"&&final.virgo.catalogPoints===0,"Solar return clears Virgo");assert(primitiveFinal===primitiveInitial&&dataSourcesFinal===dataSourcesInitial,"primitive and data-source baseline restored");const requiredErrors=consoleErrors.filter(value=>/Uncaught|TypeError|ReferenceError|RangeError|phase-4b|virgo/i.test(value)),requiredFailures=networkFailures.filter(value=>/phase-4b|virgo/i.test(value.error||""));assert(requiredErrors.length===0,"required console errors zero");assert(requiredFailures.length===0,"required network failures zero");
const report={generatedAt:new Date().toISOString(),url,initial,overviewDebug,overview,named,m87,selectedM87,card,possible,cluster,cycles,languages,mobile,primitiveInitial,primitiveFinal,dataSourcesInitial,dataSourcesFinal,final,consoleErrors,networkFailures,requiredErrors,requiredFailures,screenshots:fs.readdirSync(outputDir).filter(file=>file.endsWith(".png")).sort()};fs.writeFileSync(path.join(outputDir,"acceptance-report.json"),JSON.stringify(report,null,2)+"\n");console.log(JSON.stringify({galaxies:overviewDebug.virgo.catalogPoints,onScreen:overview.onScreen,m87:selectedM87,cycles:cycles.length,viewer:final.viewerCount,cesiumCanvas:mobile.cesiumCanvas,primitiveGrowth:primitiveFinal-primitiveInitial,dataSourceGrowth:dataSourcesFinal-dataSourcesInitial,requiredErrors:requiredErrors.length,requiredFailures:requiredFailures.length,outputDir},null,2));socket.close();
