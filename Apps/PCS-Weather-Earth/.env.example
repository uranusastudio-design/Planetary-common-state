# URL of the deployed Cloudflare worker (pcs-backend).
# Tile requests are proxied through the worker so the OpenWeather API key
# never appears in browser network traffic.
# Example: https://pcs-backend.YOUR_ACCOUNT.workers.dev
VITE_PCS_BACKEND_URL=